export enum GameStatus {
  PENDING = "PENDING",
  STARTED = "STARTED",
  FINISHED = "FINISHED",
}

export enum RoomStatus {
  ACTIVE = "ACTIVE",
  EXPIRED = "EXPIRED",
}

export enum JoinType {
  PUBLIC = "PUBLIC",
  INVITE = "INVITE",
}
